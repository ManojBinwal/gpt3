import React from 'react';
import ReactDOM  from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); //this line means that the complete app will be redered inside the div element of the index.html file with the id of root. // 

